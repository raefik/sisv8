<%= form_for(@impact_internship) do |f| %>
  <% if @impact_internship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@impact_internship.errors.count, "error") %> prohibited this impact_internship from being saved:</h2>

      <ul>
      <% @impact_internship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
