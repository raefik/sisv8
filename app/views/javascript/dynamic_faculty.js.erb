var faculties = new array();
	<% for faculty in @faculties %>

	faculties.push(new Array (<%= faculty.faculty_id, '<%=h faculty.name>',<%= faculty_id%>%>));

	<%end%>

	function facultySelected(){

		faculty_id = $ ('faculty_id').getValue();
		options = $('kampu_id').options;





	}