<%= form_for(@kampu) do |f| %>
  <% if @kampu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kampu.errors.count, "error") %> prohibited this kampu from being saved:</h2>

      <ul>
      <% @kampu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :cawangan %><br />
    <%= f.text_field :cawangan %>
  </div>
  <div class="field">
    <%= f.label :state_id %><br />
    <%= f.number_field :state_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
