<%= form_for(@code_program) do |f| %>
  <% if @code_program.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@code_program.errors.count, "error") %> prohibited this code_program from being saved:</h2>

      <ul>
      <% @code_program.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
