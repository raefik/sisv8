<%= form_for(@sesi) do |f| %>
  <% if @sesi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sesi.errors.count, "error") %> prohibited this sesi from being saved:</h2>

      <ul>
      <% @sesi.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
