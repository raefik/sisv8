<%= form_for(@technology_skill) do |f| %>
  <% if @technology_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@technology_skill.errors.count, "error") %> prohibited this technology_skill from being saved:</h2>

      <ul>
      <% @technology_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
