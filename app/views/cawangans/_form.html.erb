<%= form_for(@cawangan) do |f| %>
  <% if @cawangan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cawangan.errors.count, "error") %> prohibited this cawangan from being saved:</h2>

      <ul>
      <% @cawangan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :state_id %><br />
    <%= f.number_field :state_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
