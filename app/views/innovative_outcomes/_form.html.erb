<%= form_for(@innovative_outcome) do |f| %>
  <% if @innovative_outcome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@innovative_outcome.errors.count, "error") %> prohibited this innovative_outcome from being saved:</h2>

      <ul>
      <% @innovative_outcome.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
