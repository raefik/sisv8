<h1>Listing user_companies</h1>

<table>
  <tr>
    <th>Student</th>
    <th>Company</th>
    <th>Status</th>
    <th>Quantity</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_companies.each do |user_company| %>
  <tr>
    <td><%= user_company.student_id %></td>
    <td><%= user_company.company_id %></td>
    <td><%= user_company.action_status.name %></td>
    <td><%= user_company.quantity %></td>
    <td><%= link_to 'Show', user_company %></td>
    <td><%= link_to 'Edit', edit_user_company_path(user_company) %></td>
    <td><%= link_to 'Destroy', user_company, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User company', new_user_company_path %>
