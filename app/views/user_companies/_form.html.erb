<%= form_for(@user_company) do |f| %>
  <% if @user_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_company.errors.count, "error") %> prohibited this user_company from being saved:</h2>

      <ul>
      <% @user_company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :student_id %><br />
    <%= f.number_field :student_id %>
  </div>
  <div class="field">
    <%= f.label :company_id %><br />
    <%= f.number_field :company_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%=collection_select(:user_company,:status,Status.all,:id,:name)%>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
