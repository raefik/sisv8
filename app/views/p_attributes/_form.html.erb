<%= form_for(@p_attribute) do |f| %>
  <% if @p_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@p_attribute.errors.count, "error") %> prohibited this p_attribute from being saved:</h2>

      <ul>
      <% @p_attribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
