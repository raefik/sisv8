<%= form_for(@sem_internship) do |f| %>
  <% if @sem_internship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sem_internship.errors.count, "error") %> prohibited this sem_internship from being saved:</h2>

      <ul>
      <% @sem_internship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
