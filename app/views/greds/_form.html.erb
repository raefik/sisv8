<%= form_for(@gred) do |f| %>
  <% if @gred.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gred.errors.count, "error") %> prohibited this gred from being saved:</h2>

      <ul>
      <% @gred.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
